extensions:
      - PhpSpec\Laravel\Extension\LaravelExtension

suites:
    acme_suite:
#    the namespace is created here for reference. If we want to make a new set of information to test, create a namespace appropriate.
        namespace: Acme

        src_path: app

        spec_path: app

        spec_prefix: spec